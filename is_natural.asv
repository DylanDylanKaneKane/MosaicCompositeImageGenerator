function is_natural = is_natural(image, image_directory)

TF = startsWith(image, 'Image\training\
if image_directory == 'Image\training\natural'
    is_natural = true;
else if 
    is_natural = false;
    
    is_natural
end